import $ from "jquery";
import {gsap} from "gsap";

$(document).ready(function(){

  //var tl = gsap.timeline();
  var redBox = $('.red-box');
  var blueBox = $('.blue-box');
  var orangeBox = $('.orange-box');
  var boxArray = [redBox, blueBox, orangeBox];

  //Wrap Utility
  //var color = gsap.utils.wrap([]);

  console.log('Page loaded');









});//ready END
